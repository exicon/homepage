(page homepage.app-builder.index
      (:require
        [homepage.layout :refer [route]]
        [homepage.app-builder.info]
        [homepage.app-builder.submission]))

(cell= (pr "route" route))

(def pg
  (cell= (if (= route "")
           (homepage.app-builder.info/pg)
           (homepage.app-builder.submission/pg))))
